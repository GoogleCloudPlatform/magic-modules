<%  params = sdk_op_def.request.reject {|k, v| k.start_with?("/") && k != "/"} -%>
<%  param_expressions = params.values.map {|v| v.go_parameter_expression} -%>
client.<%= sdk_op_def.go_func_name -%>(ctx, <%= param_expressions.join(", ") -%>)