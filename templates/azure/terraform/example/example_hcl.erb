<%  if with_dependencies && !example.prerequisites.nil? -%>
<%    example.prerequisites.each do |pre_ex| -%>
<%=     lines(build_hcl_from_example(pre_ex.product, pre_ex.example, resource_id_hint, name_hints, random_variables)) -%>

<%    end -%>
<%  end -%>
resource "<%= example.resource -%>" "<%= resource_id_hint -%>" {
<%= lines(build_hcl_properties(example.properties)) -%>
}