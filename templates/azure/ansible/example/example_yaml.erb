- name: <%= lines(example.description + (!name_postfix.nil? && !name_postfix.empty? ? " -- #{name_postfix}" : '')) -%>
  <%= example.resource -%>:
<%= lines(indent lines(build_yaml_properties(example.properties, 2)), 4) -%>
<%= lines("  register: #{register_name}") unless register_name.nil? || register_name.empty? -%>