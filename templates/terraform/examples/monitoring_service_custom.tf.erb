resource "google_monitoring_service" "<%= ctx[:primary_resource_id] -%>" {
  service_id = "<%= ctx[:vars]['service_id'] -%>"
  display_name = "My Custom Service <%= ctx[:vars]['service_id'] -%>"

  telemetry {
  	resource_name = "<%= ctx[:vars]['resource_name'] -%>"
  }
}
