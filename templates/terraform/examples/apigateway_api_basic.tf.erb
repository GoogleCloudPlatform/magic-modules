resource "google_api_gateway_api" "<%= ctx[:primary_resource_id] %>" {
  provider = google-beta
  name = "<%= ctx[:vars]["name"] %>"
  display_name = "MM Dev API"
  labels = {
    environment = "dev"
  }
}
