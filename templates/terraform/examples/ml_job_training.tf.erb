resource "google_ml_engine_job" "<%= ctx[:primary_resource_id] -%>" { {
  job_id = "ml_job_training"
 
  training_input {
    job_dir         = "<%= ctx[:vars]['gcs_job_dir'] -%>"
    package_uris    = ["<%= ctx[:vars]['gcs_pkg_uri'] -%>"]
    python_module   = "census_training.train"
    python_version  = "2.7"
    region          = "us-central1"
    runtime_version = "1.14"
    scale_tier      = "BASIC"
  }
}