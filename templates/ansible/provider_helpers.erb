<%
-%>
<% unless object.provider_helpers.empty? -%>
<%=
  object.provider_helpers.map do |f|
    if f == object.provider_helpers.last
      lines(compile(f))
    else
      lines(compile(f), 2)
    end
  end.join
-%>
<% end -%>
