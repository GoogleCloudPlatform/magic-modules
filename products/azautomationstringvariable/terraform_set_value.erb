<%  if output_var == 'd' -%>
if !d.Get("encrypted").(bool) {
    if quotedValue := <%= input_var -%>; quotedValue != nil {
        value, _ := strconv.Unquote(*quotedValue)
        d.Set("<%= prop_name -%>", value)
    }
}
<%  else -%>
// Terraform_Set_Value is not supported.
<%  end -%>