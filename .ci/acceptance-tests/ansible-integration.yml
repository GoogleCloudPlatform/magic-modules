platform: linux

inputs:
  - name: magic-modules-new-prs

image_resource:
  type: docker-image
  source:
    repository: python
    tag: '2-jessie'

run:
    path: magic-modules-new-prs/.ci/acceptance-tests/ansible-integration.sh
