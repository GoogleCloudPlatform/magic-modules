# Archived: TGC agentic track

Moved here from the top level of `.agents/` on 2026-07-09. This track (Terraform Google Conversion
workflows, skills, scripts, rules, and task template) is **parked**: it was built around a specific
working setup and is not maintained as a general entry point while coverage work takes priority.

The files are preserved as-is. Internal references between them still assume their old locations
(`.agents/skills/tgc/`, `.agents/scripts/tgc/`, `.agents/TGC_WORKFLOWS.md`, etc.) — if this track is
revived, either move the files back or update the paths.
