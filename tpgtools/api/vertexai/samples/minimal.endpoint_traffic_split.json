{
  "endpoint": "{{ref:minimal.endpoint.json:name}}",
  "project": "{{project}}",
  "location": "{{region}}",
  "trafficSplit": [
    {
      "deployedModelId": "{{ref:minimal.model_deployment.json:deployedModelId}}",
      "trafficPercentage": 100
    }
  ]
}
