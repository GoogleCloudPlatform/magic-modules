{
  "endpoint": "{{ref:minimal.endpoint.json:name}}",
  "project": "{{project}}",
  "location": "{{region}}",
  "trafficSplit": [
    {
      "deployedModelId": "{{ref:minimal.model_deployment.json:id}}",
      "trafficPercentage": 100
    }
  ]
}
