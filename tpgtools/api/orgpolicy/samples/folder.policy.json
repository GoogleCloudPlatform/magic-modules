{
  "name": "{{ref:basic.cloudresourcemanager.folder.json:name}}/policies/gcp.resourceLocations",
  "parent": "{{ref:basic.cloudresourcemanager.folder.json:name}}",
  "spec": {
    "rules": [
      {
        "denyAll": true
      }
    ],
    "inheritFromParent": true
  }
}
