<% autogen_exception -%>
package runtimeconfigservice

<% unless version == 'ga' -%>

import (
  "fmt"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	"github.com/hashicorp/terraform-provider-google<%= "-" + version unless version == 'ga'  -%>/google<%= "-" + version unless version == 'ga'  -%>"
)

func DataSourceGoogleRuntimeconfigVariable() *schema.Resource {

	dsSchema := google.DatasourceSchemaFromResourceSchema(ResourceRuntimeconfigVariable().Schema)
	google.AddRequiredFieldsToSchema(dsSchema, "name")
	google.AddRequiredFieldsToSchema(dsSchema, "parent")
	google.AddOptionalFieldsToSchema(dsSchema, "project")

	return &schema.Resource{
		Read:   dataSourceGoogleRuntimeconfigVariableRead,
		Schema: dsSchema,
	}
}

func dataSourceGoogleRuntimeconfigVariableRead(d *schema.ResourceData, meta interface{}) error {
	config := meta.(*google.Config)

	id, err := google.ReplaceVars(d, config, "projects/{{project}}/configs/{{parent}}/variables/{{name}}")
	if err != nil {
		return fmt.Errorf("Error constructing id: %s", err)
	}
	d.SetId(id)
	return resourceRuntimeconfigVariableRead(d, meta)
}

<% end -%>
