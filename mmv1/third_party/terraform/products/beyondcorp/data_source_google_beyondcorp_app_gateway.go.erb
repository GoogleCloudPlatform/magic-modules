<% autogen_exception -%>
package beyondcorpservice

import (
	"fmt"

	google "terraform-provider-google/internal"
	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
)

func DataSourceGoogleBeyondcorpAppGateway() *schema.Resource {

	dsSchema := google.DatasourceSchemaFromResourceSchema(ResourceBeyondcorpAppGateway().Schema)

	google.AddRequiredFieldsToSchema(dsSchema, "name")

	google.AddOptionalFieldsToSchema(dsSchema, "project")
	google.AddOptionalFieldsToSchema(dsSchema, "region")

	return &schema.Resource{
		Read:   dataSourceGoogleBeyondcorpAppGatewayRead,
		Schema: dsSchema,
	}
}

func dataSourceGoogleBeyondcorpAppGatewayRead(d *schema.ResourceData, meta interface{}) error {
	config := meta.(*google.Config)

	name := d.Get("name").(string)

	project, err := google.GetProject(d, config)
	if err != nil {
		return err
	}

	region, err := google.GetRegion(d, config)
	if err != nil {
		return err
	}

	d.SetId(fmt.Sprintf("projects/%s/locations/%s/appGateways/%s", project, region, name))

	return resourceBeyondcorpAppGatewayRead(d, meta)
}
