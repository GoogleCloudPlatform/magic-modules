<% autogen_exception -%>
package runtimeconfigservice

<% unless version == 'ga' -%>
import (
	"fmt"

	"github.com/hashicorp/terraform-plugin-sdk/v2/helper/schema"
	google "terraform-provider-google/internal"
)

func DataSourceGoogleRuntimeconfigConfig() *schema.Resource {

	dsSchema := google.DatasourceSchemaFromResourceSchema(ResourceRuntimeconfigConfig().Schema)
	google.AddRequiredFieldsToSchema(dsSchema, "name")
	google.AddOptionalFieldsToSchema(dsSchema, "project")

	return &schema.Resource{
		Read:   dataSourceGoogleRuntimeconfigConfigRead,
		Schema: dsSchema,
	}
}

func dataSourceGoogleRuntimeconfigConfigRead(d *schema.ResourceData, meta interface{}) error {
	config := meta.(*google.Config)

	id, err := google.ReplaceVars(d, config, "projects/{{project}}/configs/{{name}}")
	if err != nil {
		return fmt.Errorf("Error constructing id: %s", err)
	}
	d.SetId(id)
	return resourceRuntimeconfigConfigRead(d, meta)
}
<% end %>
