<% autogen_exception -%>
# Google Cloud Platform Ansible Collection
This collection provides a series of Ansible modules and plugins for interacting with the [Google Cloud Platform](https://cloud.google.com)

This collection works with Ansible 2.9+

# Installation
```bash
ansible-galaxy collection install google.cloud
```

# Resources Supported
<% products.each do |prod| -%>
<%
  prod[:definitions].objects.each do |obj|
  next if obj.exclude || obj.not_in_version?(prod[:definitions].version_obj_or_closest(version))
-%>
  * <%= prod[:definitions].display_name -%> <%= obj.name -%> (<%= module_name(obj) -%>, <%= module_name(obj) -%>_facts)
<% end -%>
<% end -%>
