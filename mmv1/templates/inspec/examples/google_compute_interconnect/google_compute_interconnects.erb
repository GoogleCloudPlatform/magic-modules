<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
<%# interconnect = grab_attributes(pwd)['interconnect'] -%>
describe google_compute_interconnects(project: <%= doc_generation ? "#{gcp_project_id}" : "gcp_project_id" -%>) do
  it { should exist }
end