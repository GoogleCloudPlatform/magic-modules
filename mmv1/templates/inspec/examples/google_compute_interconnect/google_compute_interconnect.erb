<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
describe google_compute_interconnect(project: <%= doc_generation ? "#{gcp_project_id}" : "gcp_project_id" -%>) do
  it { should exist }
end