<% gcp_project_id = "#{external_attribute(pwd, 'gcp_project_id', doc_generation)}" -%>
<% external_vpn_gateway = grab_attributes(pwd)['external_vpn_gateway'] -%>
describe google_compute_external_vpn_gateway(project: <%= gcp_project_id -%>, name: <%= doc_generation ? "'#{external_vpn_gateway['name']}'" : "external_vpn_gateway['name']" -%>) do
  it { should exist }
  it { should be_up }
end