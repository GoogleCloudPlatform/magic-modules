err = ComputeOperationWaitTime(
	config, res, project, "RegionNetworkFirewallPolicyRule operation", userAgent,
    d.Timeout(schema.TimeoutCreate))

if err != nil {
	// The resource didn't actually worked
	d.SetId("")
	return fmt.Errorf("Error waiting for RegionNetworkFirewallPolicyRule operation: %s", err)
}