func defaultProviderCustomizeDiff(_ context.Context, diff *schema.ResourceDiff, meta interface{}) error {
	config := meta.(*Config)

	project, err := config.NewResourceManagerClient(config.UserAgent).Projects.Get(pid).Do()
	projecttwo, err := getProjectFromDiff(diff, config)
	if project != nil {
		log.Printf(Project1 pid: %s, err: %s", project)
	}
	if projecttwo != nil {
		log.Printf(Project2 pid: %s, err: %s", projecttwo)
	}
	if err != nil {
		return fmt.Errorf("Failed to retrieve project, pid: %s, err: %s", project, err)
	}

	return nil
}