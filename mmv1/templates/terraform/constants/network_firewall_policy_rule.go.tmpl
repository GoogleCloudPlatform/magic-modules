// Match a string that starts with one / slash followed .* t then suppress it as API takes // or / as prefixes
func SecurityProfileGroupPrefixSlashes(_, old, new string, _ *schema.ResourceData) bool {
	re := regexp.MustCompile(`^/[^/].*`)
	if re.MatchString(new) {
		return true
	}
	return old == new
}