resource "google_storage_bucket" "<%= ctx[:primary_resource_id] %>" {
  name                        = "<%= ctx[:vars]['name'] %>"
  location                    = "US"
  uniform_bucket_level_access = true
}
