resource "google_blockchain_node_engine_blockchain_nodes" "<%= ctx[:primary_resource_id] %>" {
  location = "us-central1"
  blockchain_type = "ETHEREUM"
  blockchain_node_id = "test"
  ethereum_details {
    node_type = "ARCHIVE"
    consensus_client = "LIGHTHOUSE"
    execution_client = "ERIGON"
    network = "MAINNET"
  }
  labels = {
    environment = "dev"
  }
}
