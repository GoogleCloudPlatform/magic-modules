resource "google_compute_public_delegated_sub_prefix" "{{$.PrimaryResourceId}}" {
  name = "{{$.Vars.sub_prefix_name}}"
  region = "us-central1"
  description = "Public sub delegated prefix for testing "
  ip_cidr_range = "192.0.2.0/24"
  parent_prefix = "{{$.TestEnvVars.parent_prefix}}"
  delegated_project = {{$.TestEnvVars.delegated_project}}
  public_delegated_prefix = {{$.TestEnvVars.public_delegated_prefix}}
}