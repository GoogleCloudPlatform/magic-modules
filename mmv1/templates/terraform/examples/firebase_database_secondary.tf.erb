resource "google_firebase_database" "secondary" {
  provider = google-beta
  project  = "<%= ctx[:test_env_vars]['project_id'] %>"
  region   = "<%= ctx[:vars]['region'] %>"
  database_id = "<%= ctx[:test_env_vars]['project_id'] %>-secondary-rtdb"
}
