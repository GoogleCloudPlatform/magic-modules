# [START compute_custom_hostname_instace_create]

resource "google_compute_instance" "<%= ctx[:primary_resource_id] %>" {
  name         = "<%= ctx[:vars]['custom_hostname_instance_name'] %>"
  machine_type = "f1-micro"
  provider = google

  # Set a custom hostname below 
  hostname = "hashicorptest.com."
  
  boot_disk {
    initialize_params {
      image = "debian-cloud/debian-9"
    }
  }


  network_interface {
    # A default network is created for all GCP projects
    network = "default"
    access_config {
    }
  }
}

# [END compute_custom_hostname_instace_create]
