resource "google_bigquery_biglake_catalog" "<%= ctx[:primary_resource_id] %>"  {
    catalog_id = "<%= ctx[:vars]['catalog_id'] %>"
    project = "<%= ctx[:vars]['project'] %>"
    location = "<%= ctx[:vars]['location'] %>"
}
