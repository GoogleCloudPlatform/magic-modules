resource "google_biglake_catalog" "<%= ctx[:primary_resource_id] %>"  {
    catalog_id = "<%= ctx[:vars]['catalog_id'] %>"
    project = "<%= ctx[:vars]['project'] %>"
    # Hard code to avoid invalid random id suffix
    location = "US"
}
