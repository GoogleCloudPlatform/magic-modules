resource "google_firebase_database" "default" {
  provider = google-beta
  project  = "<%= ctx[:test_env_vars]['project_id'] %>"
  region   = "<%= ctx[:vars]['region'] %>"
  database_id = "<%= ctx[:test_env_vars]['project_id'] %>-default-rtdb"
}
