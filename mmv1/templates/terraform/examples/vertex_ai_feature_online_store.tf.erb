resource google_vertex_ai_feature_online_store "<%= ctx[:primary_resource_id] %>" {
    name = "<%= ctx[:vars]['name'] %>"
    region = "us-central1"
    labels = {
        label-one = "value-one"
    }

    bigtable {
        auto_scaling {
            min_node_count = 1
            max_node_count = 2
            cpu_utilization_target = 60
        }
    }
}
