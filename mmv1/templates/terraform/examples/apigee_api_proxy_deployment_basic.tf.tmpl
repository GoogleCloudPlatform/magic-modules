resource "google_apigee_api_proxy_deployment" "{{$.PrimaryResourceId}}" {
  org               = "{{index $.Vars "apigee_org"}}"
  environment       = "dev"
  api               = "hello-proxy"
  revision          = 1
  override          = true
  sequenced-rollout = true
}