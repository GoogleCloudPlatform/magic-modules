resource "google_vertex_ai_metadata_schema" "<%= ctx[:primary_resource_id] %>"{
  name = "<%= ctx[:vars]['name'] %>"
  description = "Schema to test the terraform module"
  schema = "schema"
  schemaVersion = "0.0.1"
  schemaType = "ARTIFACT_TYPE"
}