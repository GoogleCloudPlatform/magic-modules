resource "google_identity_platform_default_supported_idp_config" "{{$.PrimaryResourceId}}" {
  enabled       = true
  idp_id        = "playgames.google.com"
  client_id     = "client-id"
  client_secret = "secret"
}
