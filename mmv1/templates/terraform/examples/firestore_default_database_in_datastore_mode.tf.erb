resource "google_firestore_database" "<%= ctx[:primary_resource_id] %>" {
  project     = "<%= ctx[:test_env_vars]['project_id'] %>"
  name        = "(default)"
  location_id = "nam5"
  type        = "DATASTORE_MODE"
}
