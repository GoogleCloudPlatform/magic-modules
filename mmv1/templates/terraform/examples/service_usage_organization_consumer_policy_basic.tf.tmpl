resource "google_service_usage_v2_organization_consumer_policy" "{{$.PrimaryResourceId}}" {
  provider     = google-beta
  organization = "{{index $.TestEnvVars "org_id"}}"
  force        = true

  enable_rules {
    services = [
      "services/storage.googleapis.com"
    ]
  }
}
