resource "google_compute_network_edge_security_services" "<%= ctx[:primary_resource_id] %>" {
  name     = "<%= ctx[:vars]['resource_name'] %>"
  region = "us-central1"
  description  = "My basic resource"
}