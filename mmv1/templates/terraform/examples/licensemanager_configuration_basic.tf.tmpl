resource "google_licensemanager_configuration" "{{$.PrimaryResourceId}}" {
  location         = "us-central1"
  configuration_id = "{{index $.Vars "configuration_id"}}"
  product          = "{{index $.Vars "product"}}"
  count            = {{index $.Vars "count"}}
}
