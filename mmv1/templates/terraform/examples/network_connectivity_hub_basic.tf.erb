resource "google_network_connectivity_hub" "primary" {
 name        = "<%= ctx[:vars]['resource_name'] %>"
 description = "A sample hub"
 project     = "us-central1"
 labels = {
    label-one = "value-one"
  }
}
