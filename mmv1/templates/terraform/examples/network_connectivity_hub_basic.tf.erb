resource "google_network_connectivity_hub" "<%= ctx[:primary_resource_id] %>"  {
 name        = "<%= ctx[:vars]['resource_name'] %>"
 description = "A sample hub"
 project     = "{{index $.TestEnvVars "project"}}"
 labels = {
    label-one = "value-one"
  }
}
