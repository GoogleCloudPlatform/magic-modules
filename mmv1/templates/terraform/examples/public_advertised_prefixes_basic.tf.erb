# Used to retrieve project_number later
data "google_project" "project" {
}

resource "google_compute_public_advertised_prefixes" "<%= ctx[:primary_resource_id] %>" {
  name = "<%= ctx[:vars]['prefixes_name'] %>"
  description = "hello"
  dns_verification_ip = "203.0.113.144"
  ip_cidr_range = "203.0.113.0/24"
  public_advertised_prefixs {
    name = "test"
    project = data.google_project.project.number
  }
}
