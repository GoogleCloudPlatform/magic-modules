config := meta.(*transport_tpg.Config)

if err := tpgresource.ParseImportId([]string{
    "(?P<parent>.+)/capabilityConfigs/(?P<capability_config_id>[^/]+)",
    "(?P<parent>.+)/(?P<capability_config_id>[^/]+)",
}, d, config); err != nil {
    return nil, err
}

id, err := tpgresource.ReplaceVars(d, config, "{{"{{"}}parent{{"}}"}}/capabilityConfigs/{{"{{"}}capability_config_id{{"}}"}}")
if err != nil {
    return nil, fmt.Errorf("Error constructing id: %s", err)
}
d.SetId(id)

return []*schema.ResourceData{d}, nil
