config := meta.(*transport_tpg.Config)
if err := tpgresource.ParseImportId([]string{
    "(?P<env_id>.+)/apis/(?P<api_name>[^/]+)/revisions/(?P<revision>[^/]+)",
    "(?P<env_id>.+)/(?P<api_name>[^/]+)/(?P<revision>[^/]+)",
}, d, config); err != nil {
    return nil, err
}


// Replace import id for the resource id
id, err := tpgresource.ReplaceVars(d, config, "{{"{{"}}env_id{{"}}"}}/apis/{{"{{"}}api_name{{"}}"}}/revisions/{{"{{"}}revision{{"}}"}}/deployments")
if err != nil {
    return nil, fmt.Errorf("Error constructing id: %s", err)
}
d.SetId(id)

return []*schema.ResourceData{d}, nil