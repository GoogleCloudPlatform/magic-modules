if d.Get("type") == "SECURE_WEB_GATEWAY" {
    obj["name"] = d.Get("name")
    obj["type"] = d.Get("type")
}

if obj["type"] == "SECURE_WEB_GATEWAY" && obj["routingMode"] == nil {
    return fmt.Errorf("Field routing_mode is required for Gateway of type SECURE_WEB_GATEWAY")
}