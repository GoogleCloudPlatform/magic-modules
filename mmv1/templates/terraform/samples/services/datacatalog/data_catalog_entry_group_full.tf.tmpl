resource "google_data_catalog_entry_group" "{{$.PrimaryResourceId}}" {
  entry_group_id = "{{index $.PrefixedVars "entry_group_id"}}"

  display_name = "terraform entry group"
  description = "entry group created by Terraform"
}
