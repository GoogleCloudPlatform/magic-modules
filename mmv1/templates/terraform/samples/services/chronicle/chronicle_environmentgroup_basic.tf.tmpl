resource "google_chronicle_environment_group" "{{$.PrimaryResourceId}}" {
  provider = google-beta
  location = "us"
  instance = "{{index $.TestEnvVars "chronicle_id"}}"

  display_name = "myEnvironmentGroup"
  description = "My Environment Description"
  environments_ids = [
    "Default Environment"
  ]
}
