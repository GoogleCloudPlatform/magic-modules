resource "google_chronicle_case_stage_definition" "{{$.PrimaryResourceId}}" {
  location = "us"
  instance = "{{index $.TestEnvVars "chronicle_id"}}"

  display_name = "tf_stage_%{random_suffix}"
  order = 100
}
