resource "google_chronicle_custom_list" "{{$.PrimaryResourceId}}" {
  provider = google-beta
  location = "us"
  instance = "{{index $.TestEnvVars "chronicle_id"}}"

  entity_identifier = "new_filename.bin"
  category = "Unapproved Files"
  environments = jsonencode([
    "*"
  ])
}
