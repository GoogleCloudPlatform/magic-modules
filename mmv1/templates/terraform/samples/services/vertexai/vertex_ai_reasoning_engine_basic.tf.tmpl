resource "google_vertex_ai_reasoning_engine" "{{$.PrimaryResourceId}}" {
  display_name = "{{index $.PrefixedVars "name"}}"
  description  = "A basic reasoning engine"
  region       = "us-central1"
}
