resource "google_dataproc_gdc_application_environment" "{{$.PrimaryResourceId}}" {
  application_environment_id = "{{index $.PrefixedVars "application_environment_id"}}"
  serviceinstance = "do-not-delete-dataproc-gdc-instance"
  project         = "{{index $.PrefixedVars "project"}}"
  location        = "us-west2"
  namespace = "default"
}
