resource "google_iam_principal_access_boundary_policy" "{{$.PrimaryResourceId}}" {
  organization   = "{{index $.TestEnvVars "org_id"}}"
  location       = "global"
  display_name   = "{{index $.PrefixedVars "display_name"}}"
  principal_access_boundary_policy_id = "{{index $.PrefixedVars "pab_id"}}"
}
