resource "google_container_analysis_note" "{{$.PrimaryResourceId}}" {
  name = "{{index $.PrefixedVars "note_name"}}"
  attestation_authority {
    hint {
      human_readable_name = "Attestor Note"
    }
  }
}
