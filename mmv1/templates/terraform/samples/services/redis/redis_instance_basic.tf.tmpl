resource "google_redis_instance" "{{$.PrimaryResourceId}}" {
  name           = "{{index $.PrefixedVars "instance_name"}}"
  memory_size_gb = 1

  lifecycle {
    prevent_destroy = {{index $.PrefixedVars "prevent_destroy"}}
  }
}
