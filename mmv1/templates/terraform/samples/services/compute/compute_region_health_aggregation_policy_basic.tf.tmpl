data "google_project" "project" {
  provider = google-beta
}

resource "google_compute_region_health_aggregation_policy" "{{$.PrimaryResourceId}}" {
  provider    = google-beta
  name        = "{{index $.PrefixedVars "name"}}"
  description = "{{index $.PrefixedVars "description"}}"
  region      = "us-central1"
}
