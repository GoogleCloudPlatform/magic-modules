resource "google_compute_interconnect_attachment_group" "{{$.PrimaryResourceId}}" {
  name   = "{{index $.PrefixedVars "interconnect_attachment_group_name"}}"
  intent {
    availability_sla = "NO_SLA"
  }
}