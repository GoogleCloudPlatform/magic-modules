resource "google_data_fusion_instance" "{{$.PrimaryResourceId}}" {
  name = "{{index $.PrefixedVars "instance_name"}}"
  region = "us-central1"
  type = "ENTERPRISE"
  enable_rbac = true
  {{index $.PrefixedVars "prober_test_run"}}
}