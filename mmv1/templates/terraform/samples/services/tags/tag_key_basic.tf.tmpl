resource "google_tags_tag_key" "{{$.PrimaryResourceId}}" {
  parent = "organizations/{{index $.TestEnvVars "org_id"}}"
  short_name = "{{index $.ResourceIdVars "short_name"}}"
  description = "For {{index $.ResourceIdVars "short_name"}} resources."
}
