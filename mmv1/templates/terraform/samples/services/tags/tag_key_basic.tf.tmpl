resource "google_tags_tag_key" "{{$.PrimaryResourceId}}" {
  parent = "organizations/{{index $.TestEnvVars "org_id"}}"
  short_name = "{{index $.PrefixedVars "short_name"}}"
  description = "For {{index $.PrefixedVars "short_name"}} resources."
}
