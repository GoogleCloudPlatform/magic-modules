resource "google_bigquery_connection" "{{$.PrimaryResourceId}}" {
   connection_id = "{{index $.PrefixedVars "connection_id"}}"
   location      = "US"
   friendly_name = "👋"
   description   = "a riveting description"
   cloud_spanner { 
      database = "{{index $.PrefixedVars "database"}}"
      database_role = "{{index $.PrefixedVars "database_role"}}"
   }
}