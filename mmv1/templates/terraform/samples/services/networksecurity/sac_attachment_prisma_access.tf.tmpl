resource "google_network_security_sac_attachment" "{{$.PrimaryResourceId}}" {
    provider = google-beta
    name = "{{index $.PrefixedVars "resource_name"}}"
    location = "us-central1"
    labels = {
        label-one = "value-one"
    }
    ncc_gateway = "{{index $.PrefixedVars "ncc_gateway"}}"
    sac_realm = "{{index $.PrefixedVars "sac_realm"}}"
}