// When creating more than one rule sometimes the creation conflicts
// Workaround: Get actual time Nanosecond, reverse the number and use as Microseconds
// https://github.com/hashicorp/terraform-provider-google/issues/20892
microNow := reverse_int(time.Now().Nanosecond())
time.Sleep(time.Duration(microNow) * time.Microsecond)
