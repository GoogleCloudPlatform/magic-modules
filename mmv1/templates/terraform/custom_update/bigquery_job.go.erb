// The only updatable value is currently 'terraform_labels', which doesn't need to make any GCP calls to update this.
log.Printf("[DBBUG] config %#v is not used.", config)
return resourceBigQueryJobRead(d, meta)