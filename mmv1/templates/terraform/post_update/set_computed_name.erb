// `name` is autogenerated from the api so needs to be set post-update
name, ok := res["name"]
if !ok {
  respBody, ok := res["response"]
  if !ok {
    return fmt.Errorf("Update response didn't contain critical fields. Update may not have succeeded.")
  }

  name, ok = respBody.(map[string]interface{})["name"]
  if !ok {
    return fmt.Errorf("Update response didn't contain critical fields. Update may not have succeeded.")
  }
}
if err := d.Set("name", name.(string)); err != nil {
  return fmt.Errorf("Error setting name: %s", err)
}
d.SetId(name.(string))
