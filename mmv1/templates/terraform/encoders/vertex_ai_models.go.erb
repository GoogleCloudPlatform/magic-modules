newObj := make(map[string]interface{})
name := ""
if v, ok := d.GetOk("model_id"); ok {
	name = v.(string)
    delete(obj,"modelId")
} else {
	name = d.Get("name").(string)
}
newObj["modelId"] = name
newObj["model"] = obj
if v, ok := d.GetOk("model_id"); ok {
    delete(newObj,"parentModel")
}
return newObj, nil