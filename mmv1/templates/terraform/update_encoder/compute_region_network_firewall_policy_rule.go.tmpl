if !d.HasChange("match") {
  return obj, nil
}
oldMatch, newMatch := d.GetChange("match")

err := adjustRegionNetworkFirewallPolicyRuleNetworkContextFields(obj, oldMatch, newMatch)
if err != nil {
  return obj, err
}

return obj, nil
