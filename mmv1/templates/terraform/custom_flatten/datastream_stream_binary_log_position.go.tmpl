func flatten{{$.GetPrefix}}{{$.TitlelizeProperty}}(v interface{}, d *schema.ResourceData, config *transport_tpg.Config) interface{} {
	if v != nil {
		return []interface{}{make(map[string]interface{})}
	}
	// The API omits binaryLogPosition from responses even when it is active, because it is
	// the default CDC method for MySQL streams. Only gtid is returned explicitly when active.
	if gtid, ok := d.GetOk("source_config.0.mysql_source_config.0.gtid"); ok && len(gtid.([]interface{})) > 0 {
		return []interface{}{}
	}
	return []interface{}{make(map[string]interface{})}
}
