apiVersion: core.cnrm.cloud.google.com/v1alpha1
kind: ServiceMapping
metadata:
  name: bigquerydatatransfer.cnrm.cloud.google.com
  namespace: cnrm-system
spec:
  name: BigqueryDataTransfer
  version: v1alpha1
  resources:
    - name: google_bigquery_data_transfer_config
      kind: BigqueryDataTransferConfig
      idTemplate: "{{name}}"
      metadataMapping:
        name: name
