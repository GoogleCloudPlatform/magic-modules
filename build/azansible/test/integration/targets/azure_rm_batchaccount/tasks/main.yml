---
# ----------------------------------------------------------------------------
#
#     ***     AUTO GENERATED CODE    ***    AUTO GENERATED CODE     ***
#
# ----------------------------------------------------------------------------
#
#     This file is automatically generated by Magic Modules and manual
#     changes will be clobbered when the file is regenerated.
#
#     Please read more about how to change this file at
#     https://www.github.com/GoogleCloudPlatform/magic-modules
#
# ----------------------------------------------------------------------------
# Pre-test setup
  - name: Create (or update) Batch Account
    azure_rm_batchaccount:
      resource_group: MyResGroup
      name: "{{ resource_name }}"
      location: West US
      auto_storage_account: MyStorageAccountId
      state: absent
#----------------------------------------------------------
  - name: Create (or update) Batch Account
    azure_rm_batchaccount:
      resource_group: MyResGroup
      name: "{{ resource_name }}"
      location: West US
      auto_storage_account: MyStorageAccountId
      state: present
  register: result
- name: assert changed is true
  assert:
    that:
      - result.changed == true
- name: verify that batch_account was created
  gcp_zbatchaccount_batch_account_facts:
      project: "{{ gcp_project }}"
      auth_kind: "{{ gcp_cred_kind }}"
      service_account_file: "{{ gcp_cred_file }}"
      scopes:
        - NotUsedInAzure
  register: results
- name: verify that command succeeded
  assert:
    that:
      - results['items'] | length >= 1
# ----------------------------------------------------------------------------
  - name: Create (or update) Batch Account
    azure_rm_batchaccount:
      resource_group: MyResGroup
      name: "{{ resource_name }}"
      location: West US
      auto_storage_account: MyStorageAccountId
      state: present
  register: result
- name: assert changed is false
  assert:
    that:
      - result.changed == false
#----------------------------------------------------------
  - name: Create (or update) Batch Account
    azure_rm_batchaccount:
      resource_group: MyResGroup
      name: "{{ resource_name }}"
      location: West US
      auto_storage_account: MyStorageAccountId
      state: absent
  register: result
- name: assert changed is true
  assert:
    that:
      - result.changed == true
- name: verify that batch_account was deleted
  gcp_zbatchaccount_batch_account_facts:
      project: "{{ gcp_project }}"
      auth_kind: "{{ gcp_cred_kind }}"
      service_account_file: "{{ gcp_cred_file }}"
      scopes:
        - NotUsedInAzure
  register: results
- name: verify that command succeeded
  assert:
    that:
      - results['items'] | length == 0
# ----------------------------------------------------------------------------
  - name: Create (or update) Batch Account
    azure_rm_batchaccount:
      resource_group: MyResGroup
      name: "{{ resource_name }}"
      location: West US
      auto_storage_account: MyStorageAccountId
      state: absent
  register: result
- name: assert changed is false
  assert:
    that:
      - result.changed == false
